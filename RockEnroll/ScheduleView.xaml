<UserControl x:Class="RockEnroll.ScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RockEnroll"
             mc:Ignorable="d" 
             Height="342" Width="854">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="303*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="CourseInfo" Width="419*"/>
            <ColumnDefinition x:Name="TimeSlot" Width="310*"/>
            <ColumnDefinition Width="125*"/>
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition x:Name="CourseCode"/>
                <RowDefinition x:Name="LecTutLab"/>
            </Grid.RowDefinitions>

        </Grid>


    </Grid>

</UserControl>